import React from 'react'
import ReactDOM from 'react-dom/client'
import { AnimeArea } from './components/AnimeArea'



ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AnimeArea/>
  </React.StrictMode>,
)
